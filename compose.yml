services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped

  bot:
    build: .
    restart: unless-stopped
    env_file:
      - /opt/AnonCuteValentine-bot/.env
    volumes:
      - /opt/AnonCuteValentine-data:/app/data
    depends_on:
      - redis
